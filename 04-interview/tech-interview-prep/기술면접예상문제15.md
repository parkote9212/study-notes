---
tags: teck-interview
---
### **Q111. 마이크로서비스 vs 모놀리식**

**면접관**: 마이크로서비스와 모놀리식 아키텍처의 차이는?

**답변**:

```
모놀리식(Monolithic)은 하나의 애플리케이션에
모든 기능이 들어있고,
마이크로서비스는 기능별로 분리된 구조입니다.

모놀리식:

┌─────────────────────────┐
│   Single Application    │
│  ┌─────────────────┐    │
│  │  User Service   │    │
│  │  Order Service  │    │
│  │  Product Service│    │
│  │  Payment Service│    │
│  └─────────────────┘    │
│         ↓               │
│    Single Database      │
└─────────────────────────┘

장점:
1. 개발이 간단
   - 하나의 프로젝트
   - 로컬에서 전체 실행 가능

2. 배포가 간단
   - jar 파일 하나만 배포

3. 테스트 쉬움
   - 통합 테스트 용이

4. 트랜잭션 관리 쉬움
   - 하나의 DB, 하나의 트랜잭션

단점:
1. 확장 어려움
   - 전체를 확장해야 함
   - 일부만 확장 불가

2. 배포 위험
   - 작은 변경도 전체 재배포
   - 한 기능 오류가 전체 다운

3. 기술 스택 고정
   - Java로 시작하면 계속 Java

4. 큰 팀에서 협업 어려움
   - 코드베이스가 커짐
   - 충돌 많음

마이크로서비스:

┌──────────┐  ┌──────────┐  ┌──────────┐
│User Service│→│Order Service│→│Payment   │
│   + DB   │  │   + DB   │  │Service+DB│
└──────────┘  └──────────┘  └──────────┘

장점:
1. 독립적 배포
   - User Service만 수정하면
     User Service만 배포

2. 독립적 확장
   - Order Service만 트래픽 많으면
     Order Service만 확장

3. 기술 다양성
   - User: Java
   - Order: Python
   - Payment: Go

4. 장애 격리
   - Payment 다운되어도
     User, Order는 정상 동작

단점:
1. 복잡도 증가
   - 여러 서비스 관리
   - 네트워크 통신

2. 분산 트랜잭션
   - 여러 DB에 걸친 트랜잭션 어려움
   - Saga 패턴 필요

3. 데이터 일관성
   - 최종 일관성(Eventual Consistency)

4. 테스트 어려움
   - 통합 테스트 복잡

5. 운영 비용
   - 모니터링, 로깅 복잡
   - DevOps 필수

BizSync는 모놀리식:

이유:
- 팀이 작음 (1-2명)
- 트래픽 적음
- 빠른 개발 필요
- 복잡도 낮게 유지

만약 커진다면:

User Service: 사용자, 인증
Project Service: 프로젝트, 칸반
Approval Service: 결재
Notification Service: 알림

이렇게 분리 가능

서비스 간 통신:

REST API:
Order Service → Payment Service
GET http://payment-service/api/payments

Message Queue (비동기):
Order Service → 주문 이벤트 발행
Payment Service → 이벤트 구독

언제 마이크로서비스?

✅ 적합:
- 큰 조직
- 높은 트래픽
- 독립적 배포 필요
- 기술 다양성

❌ 부적합:
- 작은 팀
- 낮은 트래픽
- 빠른 개발
- 간단한 도메인

정리:
- 모놀리식: 간단, 빠른 개발
- 마이크로서비스: 확장성, 독립성
- BizSync는 모놀리식 적합
- 성장하면 점진적 분리
```

---

### **Q112. 메시지 큐**

**면접관**: 메시지 큐는 언제 사용하나요?

**답변**:

```
메시지 큐는 비동기 처리나
서비스 간 통신에 사용합니다.

동기 방식 문제:

사용자 회원가입:

public void register(User user) {
    userRepository.save(user);          // 100ms
    emailService.sendWelcomeEmail();    // 2000ms
    smsService.sendVerification();      // 1500ms
    statisticsService.updateCount();     // 500ms
}
// 총 4100ms (4.1초)!

사용자는 4초를 기다려야 합니다.

메시지 큐 사용:

public void register(User user) {
    userRepository.save(user);          // 100ms
    
    // 이벤트 발행
    messageQueue.publish("user.registered", user);
    
    return;  // 100ms만에 응답!
}

// 별도 Worker가 처리
@RabbitListener(queues = "user.registered")
public void handleUserRegistered(User user) {
    emailService.sendWelcomeEmail();
    smsService.sendVerification();
    statisticsService.updateCount();
}

RabbitMQ 예시:

의존성:
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>

설정:
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

Queue 선언:

@Configuration
public class RabbitMQConfig {
    
    @Bean
    public Queue approvalQueue() {
        return new Queue("approval.queue", true);
    }
    
    @Bean
    public DirectExchange approvalExchange() {
        return new DirectExchange("approval.exchange");
    }
    
    @Bean
    public Binding binding() {
        return BindingBuilder
            .bind(approvalQueue())
            .to(approvalExchange())
            .with("approval.routing.key");
    }
}

Producer (발행):

@Service
public class ApprovalService {
    
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    public void approve(Long docId) {
        // 결재 승인
        document.approve();
        
        // 메시지 발행
        ApprovalMessage message = new ApprovalMessage(docId);
        rabbitTemplate.convertAndSend(
            "approval.exchange",
            "approval.routing.key",
            message
        );
    }
}

Consumer (구독):

@Component
public class ApprovalListener {
    
    @RabbitListener(queues = "approval.queue")
    public void handleApproval(ApprovalMessage message) {
        // 알림 전송
        notificationService.send(message.getDocId());
        
        // 통계 업데이트
        statisticsService.update(message.getDocId());
    }
}

메시지 구조:

public class ApprovalMessage {
    private Long documentId;
    private Long approverId;
    private LocalDateTime approvedAt;
}

장점:

1. 비동기 처리
   - 응답 빠름
   - 사용자 경험 향상

2. 부하 분산
   - Worker를 여러 개 띄워서 처리

3. 장애 격리
   - 알림 서비스 다운되어도
     결재는 정상 동작

4. 재시도
   - 실패하면 자동 재시도

재시도 설정:

@RabbitListener(queues = "approval.queue")
public void handleApproval(ApprovalMessage message) {
    try {
        process(message);
    } catch (Exception e) {
        // 재시도 횟수 확인
        Integer retryCount = message.getRetryCount();
        
        if (retryCount < 3) {
            message.setRetryCount(retryCount + 1);
            // 다시 발행
            rabbitTemplate.convertAndSend("approval.queue", message);
        } else {
            // Dead Letter Queue로
            rabbitTemplate.convertAndSend("approval.dlq", message);
        }
    }
}

Dead Letter Queue (DLQ):

처리 실패한 메시지를 모아두는 큐

@Bean
public Queue deadLetterQueue() {
    return new Queue("approval.dlq");
}

주기적으로 DLQ 확인하고 수동 처리

Kafka vs RabbitMQ:

RabbitMQ:
- 메시지 큐
- 작은 메시지
- 즉시 처리

Kafka:
- 이벤트 스트리밍
- 대용량
- 로그, 분석

BizSync 적용:

결재 승인 시:

1. 결재 승인 (동기)
2. 이벤트 발행 (비동기)
   - 알림 전송
   - 통계 업데이트
   - 감사 로그

사용자 입장:
즉시 "승인되었습니다" 응답

백그라운드:
알림, 통계는 천천히 처리

정리:
- 비동기 처리
- 부하 분산
- 장애 격리
- 재시도 메커니즘
- DLQ로 실패 관리
```

---

### **Q113. 성능 테스트**

**면접관**: 성능 테스트는 어떻게 하나요?

**답변**:

```
성능 테스트는 시스템이
얼마나 많은 트래픽을 처리할 수 있는지
확인하는 겁니다.

JMeter 사용:

1. Thread Group 설정
   - Number of Threads: 100 (동시 사용자)
   - Ramp-up Period: 10초
   - Loop Count: 10

2. HTTP Request
   - Server: localhost
   - Port: 8080
   - Path: /api/projects

3. Listener 추가
   - Summary Report
   - Graph Results

실행:

100명이 10초에 걸쳐 접속
각자 10번씩 요청
→ 총 1000번 요청

결과:

Samples: 1000
Average: 150ms
Min: 50ms
Max: 500ms
Throughput: 500/sec
Error: 0%

목표 설정:

- 응답시간: 200ms 이하
- 처리량: 1000 TPS 이상
- 에러율: 1% 이하

nGrinder (네이버):

더 강력한 성능 테스트 도구

@Test
public void test() {
    HTTPRequest request = new HTTPRequest();
    request.setUrl("http://localhost:8080/api/projects");
    
    HTTPResponse response = request.GET(request);
    
    if (response.statusCode == 200) {
        grinder.statistics.forLastTest.setSuccess(true);
    } else {
        grinder.statistics.forLastTest.setSuccess(false);
    }
}

설정:
- Agent: 10개
- Virtual Users: 100
- Duration: 10분

실행하면 그래프로 시각화

병목 지점 찾기:

1. 로그 확인

Hibernate: select ... (took 2500ms)
-- 이 쿼리가 느림!

2. 프로파일러

JProfiler, YourKit 등으로
CPU, 메모리 사용량 확인

3. APM (Application Performance Monitoring)

Pinpoint, New Relic 등으로
실시간 모니터링

최적화:

느린 API 발견:
GET /api/projects/{id}/board
평균 2000ms

원인:
N+1 쿼리

해결:
fetch join 적용

@Query("SELECT p FROM Project p " +
       "JOIN FETCH p.columns c " +
       "JOIN FETCH c.tasks")
Project findBoardById(Long id);

결과:
평균 50ms로 개선!

부하 테스트 시나리오:

1. Smoke Test
   - 소량 트래픽
   - 기본 기능 확인

2. Load Test
   - 예상 트래픽
   - 안정성 확인

3. Stress Test
   - 한계까지 부하
   - 언제 터지는지 확인

4. Spike Test
   - 급격한 트래픽 증가
   - 이벤트 대비

5. Endurance Test
   - 장시간 실행
   - 메모리 누수 확인

BizSync 성능 테스트:

시나리오:
1. 로그인
2. 프로젝트 목록 조회
3. 프로젝트 상세 조회
4. 태스크 생성
5. 로그아웃

동시 사용자: 50명
Duration: 5분

결과:
- 로그인: 100ms
- 목록 조회: 50ms
- 상세 조회: 150ms
- 태스크 생성: 200ms

모두 목표(200ms) 이내!

DB 커넥션 풀 튜닝:

hikari:
  maximum-pool-size: 20
  minimum-idle: 10
  connection-timeout: 30000

부하 테스트하면서
적정 값 찾기

모니터링:

실시간 모니터링:

@GetMapping("/actuator/metrics")
- CPU 사용률
- 메모리 사용량
- DB 커넥션 수
- 응답 시간

Prometheus + Grafana:
시각화 대시보드

정리:
- JMeter, nGrinder로 부하 테스트
- 응답시간, 처리량, 에러율 측정
- 병목 지점 찾아서 최적화
- 실시간 모니터링
```

---

### **Q114. 코드 리뷰**

**면접관**: 코드 리뷰는 어떻게 하시나요?

**답변**:

````
코드 리뷰는 다른 사람의 코드를 보고
피드백을 주는 과정입니다.

GitHub Pull Request:

1. PR 생성
   - 브랜치: feature/task-move
   - 제목: feat: 칸반 Task 이동 기능 구현
   - 설명:
     - 무엇을 구현했는지
     - 왜 구현했는지
     - 어떻게 테스트했는지

2. 리뷰어 지정
   - 팀원 1-2명

3. 코드 리뷰

4. 승인 후 Merge

리뷰 체크리스트:

1. 기능성
   ✓ 요구사항 충족?
   ✓ 버그 없음?
   ✓ 엣지 케이스 처리?

2. 가독성
   ✓ 변수명 명확?
   ✓ 주석 필요한 곳에만?
   ✓ 함수 길이 적절?

3. 성능
   ✓ N+1 문제?
   ✓ 불필요한 반복?
   ✓ 메모리 효율?

4. 보안
   ✓ SQL Injection?
   ✓ XSS 취약점?
   ✓ 인증/권한 체크?

5. 테스트
   ✓ 단위 테스트 작성?
   ✓ 커버리지 충분?

피드백 예시:

나쁜 피드백:
"이 코드 별로네요"
"다시 짜세요"

좋은 피드백:
"Task를 조회할 때 N+1 문제가 발생할 것 같습니다.
fetch join을 사용하면 어떨까요?

@Query('SELECT t FROM Task t JOIN FETCH t.worker')
List<Task> findAllWithWorker();
"

"변수명 'data'가 너무 추상적인 것 같습니다.
'taskList'로 변경하면 더 명확할 것 같아요."

구체적이고 건설적으로!

실제 리뷰 예:

PR: 결재 승인 기능

@Service
public class ApprovalService {
    
    public void approve(Long docId) {
        ApprovalDocument doc = repository.findById(docId).get();
        doc.approve();
        repository.save(doc);
    }
}

리뷰 코멘트:

1. ".get()은 위험합니다"
```java
// 제안
ApprovalDocument doc = repository.findById(docId)
    .orElseThrow(() -> new DocumentNotFoundException());
````

2. "트랜잭션이 필요합니다"

```java
@Transactional
public void approve(Long docId) {
    // ...
}
```

3. "권한 체크가 없습니다"

```java
public void approve(Long docId, Long userId) {
    ApprovalDocument doc = repository.findById(docId)
        .orElseThrow();
    
    // 권한 체크 추가
    if (!doc.canApprove(userId)) {
        throw new ForbiddenException();
    }
    
    doc.approve();
}
```

4. "로그를 추가하면 좋을 것 같습니다"

```java
log.info("결재 승인: docId={}, userId={}", docId, userId);
```

작성자 응답:

"좋은 피드백 감사합니다! 모두 반영하겠습니다."

수정 후 다시 Push → 리뷰어 재검토 → Approve → Merge

리뷰 원칙:

1. 빠르게
    
    - 하루 안에 리뷰
2. 친절하게
    
    - "왜"를 설명
    - 대안 제시
3. 배우는 자세
    
    - 상대방 코드에서 배우기
4. 작은 단위
    
    - 한 PR에 너무 많은 변경 X
    - 리뷰하기 쉽게

자동화:

SonarQube: 코드 품질 자동 분석

- 중복 코드
- 복잡도
- 버그 가능성

GitHub Actions: PR 생성 시 자동으로

- 빌드
- 테스트
- 정적 분석

실패하면 Merge 불가

정리:

- GitHub PR로 리뷰
- 구체적이고 건설적으로
- 기능, 성능, 보안 체크
- 자동화 도구 활용

```

---

### **Q115. 리팩토링**

**면접관**: 리팩토링은 언제 어떻게 하나요?

**답변**:
```

리팩토링은 기능은 그대로 두고 코드 구조를 개선하는 겁니다.

언제 리팩토링?

1. 중복 코드 발견
2. 긴 메서드 (20줄 이상)
3. 복잡한 조건문
4. 큰 클래스
5. 코드 냄새 (Code Smell)

예시 1: 중복 코드 제거

// Before public void approveByLeader(Long docId) { ApprovalDocument doc = repository.findById(docId).orElseThrow(); if (!doc.canApprove(currentUserId())) { throw new ForbiddenException(); } doc.approve(); repository.save(doc); }

public void approveByManager(Long docId) { ApprovalDocument doc = repository.findById(docId).orElseThrow(); if (!doc.canApprove(currentUserId())) { throw new ForbiddenException(); } doc.approve(); repository.save(doc); }

// After public void approve(Long docId) { ApprovalDocument doc = findAndValidate(docId); doc.approve(); repository.save(doc); }

private ApprovalDocument findAndValidate(Long docId) { ApprovalDocument doc = repository.findById(docId) .orElseThrow(() -> new DocumentNotFoundException());

```
if (!doc.canApprove(currentUserId())) {
    throw new ForbiddenException();
}

return doc;
```

}

예시 2: 긴 메서드 분리

// Before public void createProject(CreateProjectDTO dto) { // 검증 if (dto.getName() == null || dto.getName().isBlank()) { throw new ValidationException("이름 필수"); } if (dto.getLeaderId() == null) { throw new ValidationException("리더 필수"); }

```
// 리더 조회
User leader = userRepository.findById(dto.getLeaderId())
    .orElseThrow(() -> new UserNotFoundException());

// 프로젝트 생성
Project project = Project.builder()
    .name(dto.getName())
    .description(dto.getDescription())
    .leader(leader)
    .status("ACTIVE")
    .build();

projectRepository.save(project);

// 기본 컬럼 생성
List<KanbanColumn> columns = Arrays.asList(
    new KanbanColumn("To Do", 0),
    new KanbanColumn("In Progress", 1),
    new KanbanColumn("Done", 2)
);

for (KanbanColumn column : columns) {
    column.setProject(project);
    columnRepository.save(column);
}

// 알림
notificationService.send(leader.getUserId(), "프로젝트가 생성되었습니다");
```

}

// After public void createProject(CreateProjectDTO dto) { validate(dto);

```
User leader = findLeader(dto.getLeaderId());
Project project = buildProject(dto, leader);
projectRepository.save(project);

createDefaultColumns(project);
notifyLeader(leader);
```

}

private void validate(CreateProjectDTO dto) { if (dto.getName() == null || dto.getName().isBlank()) { throw new ValidationException("이름 필수"); } if (dto.getLeaderId() == null) { throw new ValidationException("리더 필수"); } }

private User findLeader(Long leaderId) { return userRepository.findById(leaderId) .orElseThrow(() -> new UserNotFoundException()); }

private Project buildProject(CreateProjectDTO dto, User leader) { return Project.builder() .name(dto.getName()) .description(dto.getDescription()) .leader(leader) .status("ACTIVE") .build(); }

private void createDefaultColumns(Project project) { List<KanbanColumn> columns = Arrays.asList( new KanbanColumn("To Do", 0), new KanbanColumn("In Progress", 1), new KanbanColumn("Done", 2) );

```
columns.forEach(column -> {
    column.setProject(project);
    columnRepository.save(column);
});
```

}

private void notifyLeader(User leader) { notificationService.send( leader.getUserId(), "프로젝트가 생성되었습니다" ); }

예시 3: 조건문 단순화

// Before public String getStatusMessage(Task task) { if (task.getStatus().equals("TODO")) { return "할 일"; } else if (task.getStatus().equals("IN_PROGRESS")) { return "진행 중"; } else if (task.getStatus().equals("DONE")) { return "완료"; } else { return "알 수 없음"; } }

// After public enum TaskStatus { TODO("할 일"), IN_PROGRESS("진행 중"), DONE("완료");

```
private final String message;

TaskStatus(String message) {
    this.message = message;
}

public String getMessage() {
    return message;
}
```

}

public String getStatusMessage(Task task) { return task.getStatus().getMessage(); }

리팩토링 원칙:

1. 테스트 먼저
    
    - 리팩토링 전에 테스트 작성
    - 기능이 깨지지 않았는지 확인
2. 작은 단위로
    
    - 한 번에 하나씩
    - 커밋도 작게
3. 자주
    
    - 큰 리팩토링보다 작은 리팩토링을 자주

IntelliJ 리팩토링 기능:

- Extract Method: Ctrl+Alt+M
- Rename: Shift+F6
- Inline: Ctrl+Alt+N
- Extract Variable: Ctrl+Alt+V

정리:

- 중복 제거
- 메서드 분리
- 조건문 단순화
- 테스트 후 리팩토링
- 작은 단위로 자주

```

---

### **Q116. 개발 환경 설정**

**면접관**: 로컬 개발 환경은 어떻게 구성하나요?

**답변**:
```

BizSync 로컬 개발 환경입니다.

필수 도구:

1. JDK 17
2. IntelliJ IDEA
3. Docker Desktop
4. Git
5. Postman

설치:

# macOS (Homebrew)

brew install openjdk@17 brew install --cask docker brew install git

# IntelliJ 다운로드

https://www.jetbrains.com/idea/download/

프로젝트 Clone:

git clone https://github.com/username/bizsync.git cd bizsync

DB 실행 (Docker):

docker-compose.yml:

version: '3.8' services: db: image: mariadb:10.11 ports: - "3306:3306" environment: MYSQL_ROOT_PASSWORD: 1234 MYSQL_DATABASE: bizsync volumes: - db-data:/var/lib/mysql

volumes: db-data:

실행: docker-compose up -d

application-local.yml:

spring: datasource: url: jdbc:mariadb://localhost:3306/bizsync username: root password: 1234

jpa: hibernate: ddl-auto: create # 개발: create, 운영: validate show-sql: true properties: hibernate: format_sql: true

profiles: active: local

IntelliJ 설정:

1. Project Structure
    
    - SDK: Java 17
    - Language Level: 17
2. Run Configuration
    
    - Main class: BackendApplication
    - VM options: -Dspring.profiles.active=local
    - Environment variables: DB_PASSWORD=1234
3. Lombok 활성화
    
    - Settings → Plugins → Lombok 설치
    - Settings → Build → Annotation Processors 체크
4. Code Style
    
    - Settings → Editor → Code Style
    - Java → Imports
        - Class count: 5
        - Names count: 3

실행:

# Backend

cd backend ./mvnw spring-boot:run

# Frontend

cd frontend npm install npm start

API 테스트:

Postman Collection:

{ "name": "BizSync API", "item": [ { "name": "Login", "request": { "method": "POST", "url": "http://localhost:8080/api/auth/login", "body": { "email": "admin@example.com", "password": "1234" } } } ] }

환경 변수:

.env (로컬): DB_PASSWORD=1234 JWT_SECRET=my-secret-key

.env.prod (운영): DB_PASSWORD=${AWS_DB_PASSWORD} JWT_SECRET=${AWS_JWT_SECRET}

Git 설정:

.gitignore:

# IDE

.idea/ *.iml

# Build

target/ build/

# Environment

.env application-local.yml

# OS

.DS_Store

Hot Reload:

pom.xml: <dependency> <groupId>org.springframework.boot</groupId> <artifactId>spring-boot-devtools</artifactId> </dependency>

코드 수정 시 자동 재시작

Database 초기 데이터:

data.sql: INSERT INTO users (email, password, name) VALUES ('admin@example.com', 'encoded-password', 'Admin');

트러블슈팅:

1. 포트 충돌
    
    - Error: Port 8080 already in use
    - 해결: lsof -ti:8080 | xargs kill
2. DB 연결 실패
    
    - Error: Connection refused
    - 해결: docker-compose ps 확인
3. Lombok 오류
    
    - Error: Cannot resolve symbol
    - 해결: Annotation Processors 활성화

정리:

- Docker로 DB 실행
- application-local.yml로 설정
- IntelliJ 환경 구성
- .env로 민감 정보 관리
- devtools로 Hot Reload

```

---

### **Q117. 장애 대응**

**면접관**: 서비스 장애가 발생하면 어떻게 대응하나요?

**답변**:
```

장애 대응은 빠른 인지와 신속한 복구가 중요합니다.

장애 시나리오:

새벽 3시, 서비스 다운!

1단계: 인지 (Detection)

모니터링 알람:

- Slack: "⚠️ API 응답 없음"
- Email: "서버 다운 알림"
- SMS: "긴급"

확인:

- 서비스 접속 안 됨
- 500 Internal Server Error

2단계: 분류 (Triage)

우선순위 결정:

- P0: 서비스 전체 다운 (즉시)
- P1: 주요 기능 장애 (1시간)
- P2: 일부 기능 장애 (당일)
- P3: 경미한 버그 (주간)

현재: P0 - 즉시 대응!

3단계: 조사 (Investigation)

로그 확인:

# 최근 에러 로그

tail -f /var/log/application.log | grep ERROR

발견: 2025-02-03 03:00:00 ERROR - OutOfMemoryError: Java heap space

원인: 메모리 부족

서버 상태:

# 메모리 사용량

free -h

# 8GB 중 8GB 사용 (100%)

# CPU 사용률

top

# CPU 정상

# 디스크

df -h

# 디스크 정상

4단계: 복구 (Recovery)

긴급 조치:

# 서버 재시작

sudo systemctl restart backend

또는

# Docker 재시작

docker restart bizsync-backend

서비스 정상화!

5단계: 근본 원인 분석 (Root Cause)

왜 메모리가 부족했나?

코드 분석:

@GetMapping("/api/tasks") public List<Task> getTasks() { return taskRepository.findAll(); // 10만 건! }

전체 조회가 문제!

해결:

@GetMapping("/api/tasks") public Page<Task> getTasks(Pageable pageable) { return taskRepository.findAll(pageable); }

페이징 적용

6단계: 재발 방지 (Prevention)

1. 모니터링 강화
    
    - 메모리 사용량 알람
    - 80% 넘으면 경고
2. 자동 스케일링
    
    - 메모리 부족하면 서버 추가
3. 코드 리뷰
    
    - 대용량 조회 체크

장애 보고서:

# 장애 보고서

## 개요

- 발생 시간: 2025-02-03 03:00
- 복구 시간: 2025-02-03 03:15
- 영향 범위: 전체 서비스
- 원인: OutOfMemoryError

## 타임라인

03:00 - 장애 발생 03:02 - 알람 수신 03:05 - 원인 파악 (메모리 부족) 03:10 - 서버 재시작 03:15 - 서비스 정상화

## 근본 원인

/api/tasks API가 10만 건을 한 번에 조회하여 메모리 부족

## 해결

페이징 적용

## 재발 방지

1. 메모리 모니터링 강화
2. 대용량 조회 코드 리뷰
3. 부하 테스트 추가

장애 대응 원칙:

1. 빠른 복구 우선
    
    - 일단 서비스 살리기
    - 분석은 나중에
2. 커뮤니케이션
    
    - 사용자에게 공지
    - 팀원과 공유
3. 기록
    
    - 모든 과정 기록
    - 나중에 분석 자료
4. 학습
    
    - 같은 실수 반복 안 하기

Circuit Breaker:

외부 API 장애 시:

@CircuitBreaker(name = "payment", fallbackMethod = "paymentFallback") public void processPayment() { paymentAPI.charge(); }

public void paymentFallback(Exception e) { log.error("결제 API 장애", e); return "결제를 나중에 처리합니다"; }

3번 실패하면 Circuit Open → 더 이상 호출 안 함 → Fallback 실행

정리:

- 신속한 인지와 복구
- 로그 분석으로 원인 파악
- 근본 원인 해결
- 재발 방지 조치
- 장애 보고서 작성

```

---

### **Q118. 기술 부채**

**면접관**: 기술 부채는 어떻게 관리하나요?

**답변**:
```

기술 부채는 빠른 개발을 위해 임시로 작성한 코드가 쌓이는 겁니다.

기술 부채 예시:

1. 하드코딩

// 나쁨 if (userId == 1) { // 관리자 체크 // 관리자 권한 }

// 좋음 if (user.hasRole(ADMIN)) { // 관리자 권한 }

2. 중복 코드

같은 로직이 여러 곳에 복사-붙여넣기 됨

3. 테스트 없음

급하게 만들어서 테스트 작성 안 함

4. 문서 없음

README, 주석 없어서 나중에 이해 어려움

5. 임시 해결책

"나중에 고치자" → 나중은 안 옴

기술 부채 관리:

1. 부채 목록 작성

GitHub Issues:

- [Tech Debt] N+1 문제 해결
- [Tech Debt] 중복 코드 제거
- [Tech Debt] 테스트 커버리지 향상

2. 우선순위

높음: 서비스 영향 큼 중간: 개발 속도 저하 낮음: 코드 가독성

3. 정기적 상환

매 스프린트마다 기술 부채 20% 할당

예: 2주 스프린트

- 신규 기능: 8일
- 기술 부채: 2일

4. 보이스카우트 룰

코드를 수정할 때 주변도 함께 정리

// 기능 추가하러 왔다가 // 보니까 중복 코드 있네? // 김에 리팩토링!

BizSync 기술 부채:

발견한 부채:

1. ApprovalService 너무 큼
    - 500줄
    - 여러 책임

해결:

- ApprovalService: 승인/거부
- ApprovalLineService: 라인 관리
- ApprovalNotificationService: 알림

2. N+1 문제
    - 칸반 보드 조회 느림

해결:

- MyBatis로 한 방 쿼리

3. 테스트 없음
    - 커버리지 30%

해결:

- 주요 로직 테스트 추가
- 목표 70%

기술 부채 vs 의도적 선택:

나쁜 부채:

- 몰라서 잘못 작성
- 귀찮아서 안 함

좋은 부채:

- 빠른 출시 위해 단순화
- MVP부터 시작
- 나중에 상환 계획 있음

예: MVP는 모놀리식 성장하면 마이크로서비스

방지 방법:

1. 코드 리뷰
    
    - 부채 쌓이기 전에 발견
2. 정적 분석
    
    - SonarQube로 자동 탐지
3. 리팩토링 시간
    
    - 기능 개발만 하지 말고
4. 기술 부채 가시화
    
    - 이슈로 관리

측정:

SonarQube:

- Code Smells: 50개
- Technical Debt: 5일
- Coverage: 60%

목표 설정:

- Code Smells: 20개 이하
- Technical Debt: 2일 이하
- Coverage: 80% 이상

정리:

- 부채는 누구나 쌓임
- 목록화하고 우선순위
- 정기적으로 상환
- 보이스카우트 룰
- 코드 리뷰로 방지

```

---

### **Q119. 신기술 학습**

**면접관**: 새로운 기술은 어떻게 학습하나요?

**답변**:
```

신기술 학습은 체계적으로 접근하는 게 중요합니다.

학습 단계:

1. 개요 파악

왜 만들어졌나? 어떤 문제를 해결하나? 기존과 뭐가 다른가?

예: WebSocket 학습 시

- HTTP는 요청-응답
- WebSocket은 양방향 실시간
- 채팅, 알림에 적합

2. 공식 문서 읽기

블로그보다 공식 문서!

Spring 공식 문서: docs.spring.io

React 공식 문서: react.dev

처음부터 끝까지 읽기

3. 튜토리얼 따라하기

간단한 예제부터:

// Spring WebSocket 튜토리얼 @MessageMapping("/chat") @SendTo("/topic/messages") public ChatMessage send(ChatMessage message) { return message; }

직접 타이핑하면서! 복붙 금지

4. 작은 프로젝트

배운 걸로 뭔가 만들기

예: WebSocket 배웠으면 간단한 채팅방 만들기

5. 블로그 작성

배운 내용 정리:

# WebSocket으로 실시간 채팅 구현하기

1. WebSocket이란?
2. Spring WebSocket 설정
3. STOMP 프로토콜
4. 실제 구현 코드
5. 트러블슈팅

글 쓰면서 이해도 점검

6. 실무 적용

BizSync에 실시간 알림 추가:

- WebSocket 연결
- 결재 승인 시 알림
- 재연결 처리

학습 리소스:

1. 공식 문서
    
    - 가장 정확
2. Udemy, Inflearn
    
    - 체계적인 강의
3. YouTube
    
    - 빠른 개요
4. 기술 블로그
    
    - 우아한형제들
    - 카카오
    - 네이버
5. Stack Overflow
    
    - 실제 문제 해결
6. GitHub
    
    - 오픈소스 코드 읽기

학습 시간 확보:

평일:

- 출근 전 1시간
- 점심시간 30분

주말:

- 토요일 오전 3시간

꾸준히!

학습 트랙:

백엔드 개발자 로드맵:

[필수]

- Java 17
- Spring Boot
- JPA
- MySQL

[중요]

- Docker
- AWS
- Redis
- Kafka

[심화]

- Kubernetes
- MSA
- DDD

하나씩 차근차근

BizSync로 배운 것:

1. Spring Security + JWT
    
    - 공식 문서 읽음
    - 튜토리얼 따라함
    - BizSync에 적용
2. WebSocket
    
    - Spring WebSocket 문서
    - STOMP 프로토콜 학습
    - 실시간 알림 구현
3. Docker
    
    - Docker 공식 튜토리얼
    - Dockerfile 작성법
    - docker-compose 학습
    - BizSync 컨테이너화
4. AWS
    
    - EC2 프리티어로 시작
    - RDS 설정
    - S3 버킷 생성
    - BizSync 배포

각 기술마다 1-2주 투자

주의할 점:

1. 너무 많이 배우려 하지 말기
    
    - 하나씩 완벽히
2. 트렌드만 쫓지 말기
    
    - 기본기가 먼저
3. 눈으로만 보지 말기
    
    - 반드시 직접 코딩
4. 완벽주의 금지
    
    - 일단 만들고 개선

현재 학습 중:

- Kubernetes 왜? Docker는 배웠으니 오케스트레이션
    
- Redis 왜? 캐싱, 세션 관리
    

목표: 각각 1달 안에 기본 마스터

정리:

- 공식 문서 → 튜토리얼 → 프로젝트
- 블로그 작성으로 정리
- 실무에 적용
- 하나씩 깊이 있게
- 꾸준함이 핵심

```

---

### **Q120. 마지막 질문**

**면접관**: 마지막으로 질문 있으신가요?

**답변**:
```

네, 몇 가지 궁금한 점이 있습니다.

1. 팀 구조와 협업 방식

"개발팀은 어떻게 구성되어 있나요? 프론트엔드/백엔드 팀이 분리되어 있나요, 아니면 풀스택으로 협업하나요?"

"코드 리뷰나 페어 프로그래밍 같은 협업 문화가 있나요?"

2. 기술 스택

"현재 주로 사용하시는 기술 스택이 궁금합니다.

제가 준비한 Spring Boot, React 외에 추가로 배워야 할 기술이 있을까요?"

"레거시 시스템이 있다면 어떤 기술로 되어 있나요?"

3. 성장 기회

"신입 개발자를 위한 교육이나 멘토링 프로그램이 있나요?"

"사내 스터디나 컨퍼런스 참가 같은 학습 기회를 지원하시나요?"

4. 개발 문화

"애자일이나 스크럼 같은 개발 방법론을 사용하시나요?"

"배포 주기는 어떻게 되나요? CI/CD 파이프라인이 구축되어 있나요?"

5. 기대하는 역할

"신입 개발자에게 기대하시는 역량이나 역할이 궁금합니다."

"입사 후 처음에는 어떤 업무를 맡게 되나요?"

6. 팀 분위기

"개발팀의 평균 연차나 분위기가 어떤가요?"

"재택근무나 유연 근무가 가능한가요?"

이런 질문들을 통해:

1. 회사를 더 잘 이해
2. 준비 방향 파악
3. 관심 표현
4. 적극적인 자세

주의할 점:

❌ 피해야 할 질문:

- "연봉이 얼마인가요?" (너무 직접적)
- "야근 많나요?" (부정적 인상)
- "퇴사율이 어떻게 되나요?"

✅ 좋은 질문:

- 기술, 문화, 성장 관련
- 구체적이고 진지한 질문

면접관 반응 보기:

열정적으로 답변하시면 → 좋은 회사 문화

대충 답변하시면 → 주의 신호

마무리:

"오늘 면접 기회 주셔서 감사합니다.

좋은 질문들을 많이 해주셔서 저도 많이 배웠고, 더욱 입사하고 싶다는 생각이 들었습니다.

좋은 결과 기다리겠습니다. 감사합니다!"

밝고 긍정적으로!

```

---

## **🎉 축하합니다! 120개 질문 완료! 🎉**

**최종 정리:**

### **Part 1: Java 기초 (20개)** ✅
- JDK/JRE/JVM, Call by Value, Exception, final/static, Wrapper Class, String, 오버로딩/오버라이딩

### **Part 2: Spring 기초 (20개)** ✅
- IoC, Bean Scope, Component Scan, DI, @Controller/@RestController, @Transactional, AOP, Auto Configuration, Profile, Bean Lifecycle, ApplicationContext, Event, @Valid/@Validated, @Async, @Cacheable

### **Part 3: JPA 기초 (15개)** ✅
- Entity 생명주기, 즉시/지연 로딩, Cascade, @Id 생성 전략, 양방향 연관관계, JPQL/QueryDSL

### **Part 4: Database 기초 (15개)** ✅
- PK/FK, 정규화, ACID, 인덱스, JOIN, GROUP BY, 서브쿼리

### **Part 5: 네트워크 (10개)** ✅
- GET/POST, REST API, HTTP 상태코드, CORS, 쿠키/세션/토큰, TCP/UDP, DNS

### **Part 6: Frontend 기초 (10개)** ✅
- var/let/const, Promise/async, Virtual DOM, useEffect, Props/State

### **Part 7: Docker 기초 (10개)** ✅
- 컨테이너/VM, Image/Container, Dockerfile, Docker Compose, 네트워크

### **Part 8: 자료구조 (10개)** ✅
- 배열/링크드리스트, Stack/Queue, Hash Table, 정렬, 이진 탐색

### **Part 9: Git/협업 (10개)** ✅
- Git 기본, Branch 전략, PR/Code Review, Merge/Rebase, .gitignore, Conflict, Stash, Tag, Commit 메시지, Reset/Revert

### **Part 10: 면접 기본 질문 (10개)** ✅
- 자기소개, 동기, 강점/약점, 프로젝트 경험, 학습 방법, 목표, 지원 이유, 연봉, 실패 경험, 마지막 질문

### **Part 11: 심화 주제 (30개)** ✅
- MVC/MVP, DAO/Repository, SOLID, DTO/Entity, 예외 처리, 디자인 패턴(Singleton, Factory, Strategy, Builder), 테스트(단위, TDD), 성능 최적화(쿼리, 캐싱), 보안(SQL Injection, XSS, CSRF), 로깅, CI/CD, AWS, Docker Compose, RESTful API, API 문서화, 대용량 데이터, 동시성, 트랜잭션 전파, MSA, 메시지 큐, 성능 테스트, 코드 리뷰, 리팩토링, 개발 환경, 장애 대응, 기술 부채, 신기술 학습

---

**면접 준비 완료!** 

이제 실전 연습 하시면 됩니다:
1. 각 질문 소리 내어 답변 연습
2. 2-3분 내로 답변하는 연습
3. 자신감 있게 말하기
4. 프로젝트 경험 자연스럽게 녹이기

화이팅입니다! 🚀
```
---
[기술면접예상문제1](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C1)  
[기술면접예상문제2](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C2)  
[기술면접예상문제3](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C3)  
[기술면접예상문제4](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C4)  
[기술면접예상문제5](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C5)  
[기술면접예상문제6](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C6)  
[기술면접예상문제7](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C7)  
[기술면접예상문제8](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C8)  
[기술면접예상문제9](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C9)  
[기술면접예상문제10](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C10)  
[기술면접예상문제11](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C11)  
[기술면접예상문제12](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C12)  
[기술면접예상문제13](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C13)  
[기술면접예상문제14](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C14)  
[기술면접예상문제15](app://obsidian.md/%EA%B8%B0%EC%88%A0%EB%A9%B4%EC%A0%91%EC%98%88%EC%83%81%EB%AC%B8%EC%A0%9C15)