# [TDD 3/3] 고급편 - 2026년 최신 트렌드 & 베스트 프랙티스

🏷️기술 카테고리: Architecture, Testing
💡핵심키워드: #TDD, #성능최적화, #아키텍처
💼 면접 빈출도: 상
⚖️ 의사결정(A vs B): Yes
날짜: 2026년 1월 17일 오후 6:26
📅 다음 복습일: 2026년 1월 25일

# 1. Abstract: 핵심 요약

> **2026년 TDD 트렌드**는 빠른 피드백, 테스트 가독성, 유지보수성에 초점을 맞추고 있습니다. Testcontainers, 아키텍처 테스트, 계약 기반 테스트 등 최신 기법을 활용하여 더 강력한 테스트를 작성합니다.
> 

**2026년 핵심 트렌드**:

- 🐳 **Testcontainers**: 실제 환경과 동일한 테스트
- 🏛️ **아키텍처 테스트**: 계층 간 의존성 검증
- 🤝 **계약 기반 테스트**: API 계약 보장
- ⚡ **테스트 속도 최적화**: 병렬 실행, Slice Test
- 📊 **테스트 품질 측정**: Mutation Testing

---

# 2. Testcontainers: 실제 환경과 동일한 테스트

## 2.1 Testcontainers란?

**정의**: Docker 컨테이너를 활용한 테스트 환경 구축

**기존 방식의 문제점**:

```java
// ❌ H2 내장 DB 사용
@DataJpaTest
class UserRepositoryTest {
    // H2는 MySQL/PostgreSQL과 다름!
    // → 프로덕션에서 다른 결과 발생 가능
}
```

**Testcontainers 장점**:

- ✅ 프로덕션과 동일한 DB 사용
- ✅ 테스트 간 데이터 오염 방지
- ✅ 테스트 종료 시 자동 정리

---

## 2.2 설정 및 사용법

### 의존성 추가

```
// build.gradle
testImplementation "org.testcontainers:testcontainers:1.19.3"
testImplementation "org.testcontainers:mysql:1.19.3"
testImplementation "org.testcontainers:junit-jupiter:1.19.3"
```

---

### 실전 예제: MySQL Testcontainers

```java
@SpringBootTest
@Testcontainers  // Testcontainers 활성화
class UserRepositoryIntegrationTest {

    @Container  // 컨테이너 정의
    static MySQLContainer<?> mysql = new MySQLContainer<>("mysql:8.0")
            .withDatabaseName("testdb")
            .withUsername("test")
            .withPassword("test");

    @DynamicPropertySource  // 동적 속성 설정
    static void properties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", mysql::getJdbcUrl);
        registry.add("spring.datasource.username", mysql::getUsername);
        registry.add("spring.datasource.password", mysql::getPassword);
    }

    @Autowired
    private UserRepository userRepository;

    @Test
    @DisplayName("실제 MySQL에서 사용자 저장 테스트")
    void saveUser_Success() {
        // given
        User user = User.builder()
                .email("[test@example.com](mailto:test@example.com)")
                .password("1234")
                .build();

        // when
        User saved = [userRepository.save](http://userRepository.save)(user);

        // then
        assertThat(saved.getUserId()).isNotNull();
        assertThat(saved.getEmail()).isEqualTo("[test@example.com](mailto:test@example.com)");
    }
}
```

---

## 2.3 다중 컨테이너

```java
@SpringBootTest
@Testcontainers
class OrderServiceIntegrationTest {

    @Container
    static MySQLContainer<?> mysql = new MySQLContainer<>("mysql:8.0");

    @Container
    static KafkaContainer kafka = new KafkaContainer(
        DockerImageName.parse("confluentinc/cp-kafka:7.4.0")
    );

    @Container
    static GenericContainer<?> redis = new GenericContainer<>("redis:7")
            .withExposedPorts(6379);

    @DynamicPropertySource
    static void properties(DynamicPropertyRegistry registry) {
        // MySQL
        registry.add("spring.datasource.url", mysql::getJdbcUrl);
        registry.add("spring.datasource.username", mysql::getUsername);
        registry.add("spring.datasource.password", mysql::getPassword);

        // Kafka
        registry.add("spring.kafka.bootstrap-servers", kafka::getBootstrapServers);

        // Redis
        registry.add("[spring.redis.host](http://spring.redis.host)", redis::getHost);
        registry.add("spring.redis.port", redis::getFirstMappedPort);
    }

    @Test
    void complexIntegrationTest() {
        // MySQL + Kafka + Redis를 모두 사용하는 테스트
    }
}
```

---

# 3. 아키텍처 테스트 (ArchUnit)

## 3.1 ArchUnit이란?

**정의**: 아키텍처 규칙을 테스트로 검증하는 라이브러리

**목적**:

- 계층 간 의존성 규칙 검증
- 패키지 구조 검증
- 명명 규칙 검증

---

## 3.2 설정 및 사용법

```
testImplementation 'com.tngtech.archunit:archunit-junit5:1.2.1'
```

---

### 계층 의존성 규칙 테스트

```java
@AnalyzeClasses(packages = "com.bizsync.backend")
class ArchitectureTest {

    @ArchTest
    static final ArchRule 계층_의존성_규칙 = layeredArchitecture()
        .consideringAllDependencies()
        
        // 계층 정의
        .layer("Controller").definedBy("..controller..")
        .layer("Service").definedBy("..service..")
        .layer("Repository").definedBy("..repository..")
        .layer("Domain").definedBy("..domain..")
        
        // 의존성 규칙
        .whereLayer("Controller").mayNotBeAccessedByAnyLayer()
        .whereLayer("Controller").mayOnlyAccessLayers("Service", "Domain")
        .whereLayer("Service").mayOnlyAccessLayers("Repository", "Domain")
        .whereLayer("Repository").mayOnlyAccessLayers("Domain");

    @ArchTest
    static final ArchRule Controller는_Controller로_끝나야함 = classes()
        .that().resideInAPackage("..controller..")
        .should().haveSimpleNameEndingWith("Controller");

    @ArchTest
    static final ArchRule Service는_Service로_끝나야함 = classes()
        .that().resideInAPackage("..service..")
        .should().haveSimpleNameEndingWith("Service");

    @ArchTest
    static final ArchRule Repository는_Repository로_끝나야함 = classes()
        .that().resideInAPackage("..repository..")
        .should().haveSimpleNameEndingWith("Repository");
}
```

---

### 금지 의존성 테스트

```java
@ArchTest
static final ArchRule Controller는_Repository를_직접_사용하면_안됨 = 
    noClasses()
        .that().resideInAPackage("..controller..")
        .should().dependOnClassesThat().resideInAPackage("..repository..");

@ArchTest
static final ArchRule Domain은_다른_계층에_의존하면_안됨 = 
    classes()
        .that().resideInAPackage("..domain..")
        .should().onlyDependOnClassesThat().resideInAnyPackage(
            "java..",
            "javax..",
            "..domain.."
        );
```

---

# 4. 계약 기반 테스트 (Spring Cloud Contract)

## 4.1 계약 기반 테스트란?

**문제 상황**:

```
Frontend 개발자: "API가 바뀌어요! 연동이 깨졌어요!"
Backend 개발자: "저희는 문서 업데이트했는데요..."
```

**해결책**: API 계약을 코드로 정의하고 테스트로 보장

---

## 4.2 Spring Cloud Contract 예제

### 계약 정의 (Groovy)

```groovy
// src/test/resources/contracts/login.groovy
Contract.make {
    description "Successful login returns JWT tokens"
    
    request {
        method POST()
        url "/api/auth/login"
        headers {
            contentType(applicationJson())
        }
        body([
            email: "[test@bizsync.com](mailto:test@bizsync.com)",
            password: "1234"
        ])
    }
    
    response {
        status 200
        headers {
            contentType(applicationJson())
        }
        body([
            accessToken: $(consumer(regex('[A-Za-z0-9-_=]+\.[A-Za-z0-9-_=]+\.[A-Za-z0-9-_.+/=]+')),
            refreshToken: $(consumer(regex('[A-Za-z0-9-_=]+\.[A-Za-z0-9-_=]+\.[A-Za-z0-9-_.+/=]+')))
        ])
    }
}
```

---

### 자동 생성되는 테스트

```java
// 자동 생성됨!
public class LoginTest extends ContractVerifierBase {

    @Test
    public void validate_login() throws Exception {
        // given
        MockMvcRequestSpecification request = given();

        // when
        ResponseOptions response = given().spec(request)
            .header("Content-Type", "application/json")
            .body("{\"email\":\"[test@bizsync.com](mailto:test@bizsync.com)\",\"password\":\"1234\"}")
            .post("/api/auth/login");

        // then
        assertThat(response.statusCode()).isEqualTo(200);
        assertThat(response.header("Content-Type")).matches("application/json.*");
        // accessToken 및 refreshToken 형식 검증
    }
}
```

**효과**:

- API 계약 변경 시 테스트 실패
- Frontend와 Backend 간 계약 보장
- 문서화 자동화

---

# 5. 테스트 속도 최적화

## 5.1 병렬 테스트 실행

### JUnit 5 Parallel Execution

```
# [junit-platform.properties](http://junit-platform.properties)
junit.jupiter.execution.parallel.enabled = true
junit.jupiter.execution.parallel.mode.default = concurrent
junit.jupiter.execution.parallel.config.strategy = dynamic
```

```java
@Execution(ExecutionMode.CONCURRENT)  // 병렬 실행
class FastTest {

    @Test
    void test1() { ... }

    @Test
    void test2() { ... }  // test1과 동시 실행
}
```

**효과**:

- 10분 → 3분으로 단축
- CI/CD 파이프라인 속도 향상

---

## 5.2 Slice Test

### 계층별 테스트 분리

```java
// ❌ 느림: 전체 Context 로드
@SpringBootTest
class ControllerTest { ... }

// ✅ 빠름: Controller층만 테스트
@WebMvcTest(AuthController.class)
class AuthControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private AuthService authService;

    @Test
    void loginTest() throws Exception {
        // Controller 레이어만 테스트
        // Service, Repository는 Mock
    }
}

// ✅ 빠름: Repository층만 테스트
@DataJpaTest
class UserRepositoryTest {
    // JPA 관련 빈만 로드
}
```

**Slice Test 종류**:

- `@WebMvcTest`: Controller
- `@DataJpaTest`: Repository
- `@RestClientTest`: REST Client
- `@JsonTest`: JSON Serialization

---

# 6. Mutation Testing (PIT)

## 6.1 Mutation Testing이란?

**개념**: 코드를 일부러 변경(mutate)했을 때 테스트가 실패하는지 확인

**예시**:

```java
// 원본 코드
if (age > 18) {
    return true;
}

// Mutation 1: > → >=
if (age >= 18) {  // 테스트가 이걸 잡아내는가?
    return true;
}

// Mutation 2: true → false
if (age > 18) {
    return false;  // 테스트가 이걸 잡아내는가?
}
```

---

## 6.2 PIT 설정

```
// build.gradle
plugins {
    id 'info.solidsoft.pitest' version '1.15.0'
}

pitest {
    targetClasses = ['com.bizsync.backend.service.*']
    targetTests = ['com.bizsync.backend.service.*Test']
    outputFormats = ['HTML']
    timestampedReports = false
}
```

```bash
# 실행
./gradlew pitest

# 결과
>> Generated 50 mutations
>> Killed 45 mutations (90%)
>> Survived 5 mutations (10%)  # 테스트 보완 필요!
```

**Mutation Score**: 죽인 Mutation / 전체 Mutation

- 80% 이상: 우수한 테스트
- 60-80%: 양호한 테스트
- 60% 미만: 테스트 보완 필요

---

# 7. 베스트 프랙티스 모음

## 7.1 Test Fixture 관리

### Object Mother Pattern

```java
public class UserMother {
    
    public static User defaultUser() {
        return User.builder()
                .email("[test@bizsync.com](mailto:test@bizsync.com)")
                .password("encoded_1234")
                .role(Role.MEMBER)
                .build();
    }

    public static User adminUser() {
        return User.builder()
                .email("[admin@bizsync.com](mailto:admin@bizsync.com)")
                .password("encoded_admin")
                .role(Role.ADMIN)
                .build();
    }

    public static User withEmail(String email) {
        return defaultUser().toBuilder()
                .email(email)
                .build();
    }
}

// 사용
@Test
void test() {
    User user = UserMother.defaultUser();
    User admin = UserMother.adminUser();
    User custom = UserMother.withEmail("[custom@example.com](mailto:custom@example.com)");
}
```

---

### Builder Pattern

```java
public class UserTestBuilder {
    private String email = "[test@bizsync.com](mailto:test@bizsync.com)";
    private String password = "1234";
    private Role role = Role.MEMBER;

    public UserTestBuilder withEmail(String email) {
        [this.email](http://this.email) = email;
        return this;
    }

    public UserTestBuilder withRole(Role role) {
        this.role = role;
        return this;
    }

    public User build() {
        return User.builder()
                .email(email)
                .password(password)
                .role(role)
                .build();
    }
}

// 사용
@Test
void test() {
    User user = new UserTestBuilder()
            .withEmail("[custom@example.com](mailto:custom@example.com)")
            .withRole(Role.ADMIN)
            .build();
}
```

---

## 7.2 Test Double 전략

### 5가지 Test Double

```java
// 1. Dummy: 단순히 인자를 채우기 위한 객체
User dummy = new User();  // 사용되지 않음

// 2. Stub: 미리 정해진 답변만 반환
given(userRepository.findById(1L))
    .willReturn(Optional.of(user));  // 항상 같은 값

// 3. Spy: 실제 객체를 감싸서 일부만 Mock
@Spy
private UserService userService;  // 일부 메서드만 Mocking

// 4. Mock: 호출 검증과 반환값 제어
@Mock
private UserRepository userRepository;
verify(userRepository).save(any());

// 5. Fake: 실제 동작하는 가짜 구현
class FakeUserRepository implements UserRepository {
    private Map<Long, User> users = new HashMap<>();
    
    @Override
    public User save(User user) {
        users.put(user.getId(), user);
        return user;
    }
}
```

---

## 7.3 Flaky Test 방지

### 문제: 간헐적으로 실패하는 테스트

```java
// ❌ 나쁨: 시간 의존적
@Test
void test() {
    Thread.sleep(1000);  // 불안정
    assertThat(result).isNotNull();
}

// ✅ 좋음: 명시적 대기
@Test
void test() {
    await()
        .atMost(Duration.ofSeconds(5))
        .until(() -> result != null);
}
```

```java
// ❌ 나쁨: 현재 시간 의존
@Test
void test() {
    LocalDate today = [LocalDate.now](http://LocalDate.now)();  // 테스트 날짜마다 달라짐
}

// ✅ 좋음: 시간 고정
@Test
void test() {
    Clock fixedClock = Clock.fixed(
        Instant.parse("2024-01-01T00:00:00Z"),
        ZoneId.of("UTC")
    );
    LocalDate fixedDate = [LocalDate.now](http://LocalDate.now)(fixedClock);
}
```

---

## 7.4 Test Naming Convention

### 명확한 테스트 이름

```java
// 패턴 1: MethodName_StateUnderTest_ExpectedBehavior
@Test
void login_ValidCredentials_ReturnsToken() { ... }

@Test
void login_InvalidPassword_ThrowsException() { ... }

// 패턴 2: given_when_then (한글)
@Test
@DisplayName("유효한_자격증명_로그인하면_토큰을_반환한다")
void login_test1() { ... }

// 패턴 3: should_ExpectedBehavior_when_StateUnderTest
@Test
void shouldReturnToken_whenValidCredentials() { ... }
```

---

# 8. 2026년 TDD 체크리스트

## 해야 할 것 ✅

- [ ]  **Testcontainers**: 프로덕션과 동일한 환경 테스트
- [ ]  **ArchUnit**: 아키텍처 규칙 테스트로 검증
- [ ]  **병렬 테스트**: CI/CD 속도 향상
- [ ]  **Slice Test**: 계층별 테스트 분리
- [ ]  **Mutation Testing**: 테스트 품질 측정
- [ ]  **Test Fixture 패턴**: Object Mother/Builder 활용
- [ ]  **Flaky Test 방지**: 시간/랜덤 의존성 제거
- [ ]  **명확한 네이밍**: 테스트 의도 명확히

---

## 하지 말아야 할 것 ❌

- [ ]  **모든 것 Mocking**: 진짜 통합 테스트도 필요
- [ ]  **100% 커버리지 집착**: 의미 있는 테스트가 중요
- [ ]  **구현 상세 테스트**: 리팩토링 시 깨짐
- [ ]  **과도한 E2E**: 유지보수 비용 증가
- [ ]  **Sleep() 사용**: Flaky Test의 원인

---

# 9. Interview Readiness

## ▶ Q1: Testcontainers를 왜 사용하나요?

**A**: Testcontainers는 테스트 환경을 프로덕션과 동일하게 만들기 위해 사용합니다.

**기존 방식의 문제**:

- H2 내장 DB를 사용하면 MySQL/PostgreSQL과 다른 SQL 방언 차이로 프로덕션에서 문제 발생 가능
- 테스트 간 데이터 오염 문제

**Testcontainers 장점**:

1. 프로덕션과 동일한 DB로 테스트 (MySQL, PostgreSQL 등)
2. 테스트 종료 시 컨테이너 자동 삭제로 깨끗한 환경 보장
3. Docker로 다양한 외부 시스템(Redis, Kafka 등) 테스트 가능

**실무 사례**: 저희 팀에서 Testcontainers 도입 후 프로덕션 버그가 30% 감소했습니다. 특히 MySQL의 JSON 필드 처리나 트랜잭션 격리 수준 차이를 테스트에서 미리 발견할 수 있었습니다.

---

## ▶ Q2: ArchUnit으로 무엇을 테스트하나요?

**A**: ArchUnit은 아키텍처 규칙을 코드로 검증하는 라이브러리입니다.

**테스트 가능한 규칙**:

1. **계층 의존성 규칙**
    - Controller는 Service만 호출, Repository 직접 호출 금지
    - Service는 Repository 호출 가능
    - Domain은 다른 계층 의존 금지
2. **명명 규칙**
    - Controller 클래스는 "Controller"로 끝나야 함
    - Service 클래스는 "Service"로 끝나야 함
3. **패키지 구조**
    - 특정 패키지 간 순환 의존성 금지

**효과**: 코드 리뷰 없이도 아키텍처 위반 사항을 자동으로 감지하여 프로젝트 구조를 일관성 있게 유지할 수 있습니다.

---

## ▶ Q3: Mutation Testing이란 무엇인가요?

**A**: Mutation Testing은 테스트의 품질을 측정하는 기법입니다.

**동작 원리**:

1. 코드를 일부러 변경(mutate) - 예: `>` → `>=`, `+` → `-`
2. 변경된 코드로 테스트 실행
3. 테스트가 실패하면 "Killed" (좋음)
4. 테스트가 통과하면 "Survived" (나쁨, 테스트 보완 필요)

**Mutation Score = Killed / Total Mutations**

- 80% 이상: 우수한 테스트
- 60-80%: 양호한 테스트
- 60% 미만: 개선 필요

**코드 커버리지와의 차이**:

- 코드 커버리지: 코드가 실행되었는가?
- Mutation Testing: 테스트가 버그를 감지하는가?

100% 커버리지도 Mutation Score가 낮을 수 있습니다. Mutation Testing은 테스트의 실제 효과성을 측정합니다.

---

## 🔑 핵심 체크리스트

- [ ]  Testcontainers로 프로덕션 환경 모방
- [ ]  ArchUnit으로 아키텍처 규칙 검증
- [ ]  Spring Cloud Contract로 API 계약 보장
- [ ]  병렬 테스트로 속도 향상
- [ ]  Slice Test로 계층별 테스트
- [ ]  Mutation Testing으로 테스트 품질 측정
- [ ]  Object Mother/Builder 패턴 활용
- [ ]  Flaky Test 방지 기법 적용
- [ ]  명확한 Test Naming Convention
- [ ]  70-20-10 피라미드 준수

---

**작성일**: 2026-01-17  

**시리즈**: TDD 완벽 정복 (3/3)  

**이전 편**: TDD 실전편 - Spring Boot 테스트 작성법