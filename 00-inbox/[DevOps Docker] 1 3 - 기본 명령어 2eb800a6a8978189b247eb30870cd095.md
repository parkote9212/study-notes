# [DevOps Docker] 1/3 - 기본 명령어

🏷️기술 카테고리: DevOps, Docker
💡핵심키워드: #컨테이너
💼 면접 빈출도: 상
⚖️ 의사결정(A vs B): No
날짜: 2026년 1월 17일 오후 11:56
📅 다음 복습일: 2026년 1월 25일

# 1. Abstract: 핵심 요약

> **Docker**는 애플리케이션을 컨테이너로 패키징하여 어떤 환경에서도 동일하게 실행할 수 있게 하는 컨테이너 가상화 플랫폼입니다. **"내 컴퓨터에선 되는데?"를 해결**합니다.
> 

**핵심 원칙**:

- VM보다 경량: 초 단위 시작, 적은 메모리
- 이미지 기반: 한 번 빌드하면 어디서나 실행
- 격리성: 각 컨테이너는 독립적인 환경

---

# 2. 컨테이너 생애주기 관리

## 2.1 docker run - 컨테이너 생성 및 실행

이미지를 기반으로 새로운 컨테이너를 생성하고 실행합니다.

| 옵션 | 설명 | 예시 |
| --- | --- | --- |
| **`-d`** | 백그라운드 모드 (터미널 비점유) | `docker run -d nginx` |
| **`-p`** | 포트 매핑 (호스트:컨테이너) | `docker run -p 8080:80 nginx` |
| **`--name`** | 컨테이너 이름 지정 | `docker run --name my-web nginx` |
| **`-v`** | 볼륨 마운트 (호스트:컨테이너) | `docker run -v ~/data:/app/data nginx` |
| **`-e`** | 환경변수 설정 | `docker run -e DB_HOST=[localhost](http://localhost) mysql` |
| **`--rm`** | 종료 시 자동 삭제 | `docker run --rm busybox` |
| **`-it`** | 인터랙티브 터미널 | `docker run -it ubuntu bash` |

**실전 예제**:

```bash
# Nginx 웹 서버 실행
docker run -d \
  --name my-nginx \
  -p 80:80 \
  -v ~/html:/usr/share/nginx/html \
  nginx:latest

# MySQL 데이터베이스 실행
docker run -d \
  --name my-mysql \
  -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=password \
  -v mysql-data:/var/lib/mysql \
  mysql:8.0
```

---

## 2.2 docker start / stop / restart

이미 생성된 컨테이너를 제어합니다.

```bash
# 컨테이너 시작
docker start my-nginx

# 컨테이너 중지 (10초 대기 후 강제)
docker stop -t 10 my-nginx

# 컨테이너 재시작
docker restart my-nginx
```

---

## 2.3 docker rm - 컨테이너 삭제

| 옵션 | 설명 | 예시 |
| --- | --- | --- |
| **`-f`** | 실행 중 강제 삭제 | `docker rm -f my-web` |
| **`-v`** | 연결된 익명 볼륨도 삭제 | `docker rm -v my-web` |

```bash
# 중지된 컨테이너 삭제
docker rm my-nginx

# 모든 중지된 컨테이너 일괄 삭제
docker rm $(docker ps -aq -f status=exited)
```

---

# 3. 이미지 관리

## 3.1 docker pull / push

```bash
# Docker Hub에서 이미지 다운로드
docker pull nginx:latest
docker pull python:3.11-slim

# 로그인
docker login

# Docker Hub에 이미지 업로드
docker tag myapp:1.0 username/myapp:1.0
docker push username/myapp:1.0
```

---

## 3.2 docker images / rmi

```bash
# 이미지 목록 확인
docker images

# 이미지 삭제
docker rmi myapp:1.0

# 사용하지 않는 모든 이미지 삭제
docker image prune -a
```

---

# 4. 상태 확인 및 디버깅

## 4.1 docker ps - 컨테이너 목록

| 옵션 | 설명 | 예시 |
| --- | --- | --- |
| **`-a`** | 중지된 컨테이너 포함 | `docker ps -a` |
| **`-q`** | ID만 출력 | `docker ps -q` |
| **`--format`** | 출력 포맷 지정 | `docker ps --format "table {{.ID}}\t{{.Image}}"` |

```bash
# 실행 중인 컨테이너
docker ps

# 모든 컨테이너
docker ps -a
```

---

## 4.2 docker logs - 로그 확인

| 옵션 | 설명 | 예시 |
| --- | --- | --- |
| **`-f`** | 실시간 스트리밍 | `docker logs -f my-web` |
| **`--tail`** | 마지막 N줄만 | `docker logs --tail 100 my-web` |
| **`-t`** | 타임스탬프 표시 | `docker logs -t my-web` |

```bash
# 로그 확인
docker logs my-nginx

# 실시간 모니터링
docker logs -f --tail 50 my-nginx
```

---

## 4.3 docker exec - 컨테이너 내부 명령 실행

```bash
# 컨테이너 내부 셰 접속
docker exec -it my-nginx /bin/bash

# 파일 목록 확인
docker exec my-nginx ls -la /app

# root 권한으로 명령 실행
docker exec -u root my-nginx apt-get update
```

---

## 4.4 docker inspect - 상세 정보

```bash
# 컨테이너 상세 정보 (JSON)
docker inspect my-nginx

# IP 주소만 출력
docker inspect -f '{{.NetworkSettings.IPAddress}}' my-nginx

# 볼륨 정보 확인
docker inspect -f '{{json .Mounts}}' my-nginx | jq
```

---

# 5. 네트워크 관리

## 5.1 docker network

```bash
# 네트워크 목록
docker network ls

# 사용자 정의 네트워크 생성
docker network create my-net

# 네트워크 상세 정보
docker network inspect my-net

# 컨테이너를 네트워크에 연결
docker network connect my-net my-db
```

**실전 예제**:

```bash
# 1. 네트워크 생성
docker network create app-network

# 2. DB 컨테이너 실행 (my-network 연결)
docker run -d \
  --name mysql \
  --network app-network \
  -e MYSQL_ROOT_PASSWORD=password \
  mysql:8.0

# 3. 백엔드 컨테이너 실행 (같은 네트워크)
docker run -d \
  --name backend \
  --network app-network \
  -e DB_HOST=mysql \
  myapp:latest

# ✅ 백엔드에서 "mysql"로 DB 접근 가능!
```

---

# 6. 볼륨 관리

## 6.1 docker volume

```bash
# 볼륨 목록
docker volume ls

# 볼륨 생성
docker volume create my-db-data

# 볼륨 상세 정보
docker volume inspect my-db-data

# 볼륨 삭제
docker volume rm my-db-data
```

**사용 예제**:

```bash
# 볼륨을 사용하는 MySQL
docker run -d \
  --name mysql \
  -v mysql-data:/var/lib/mysql \
  mysql:8.0

# 컨테이너를 삭제해도 데이터는 볼륨에 남음!
docker rm -f mysql

# 다시 실행해도 데이터 보존됨
docker run -d --name mysql -v mysql-data:/var/lib/mysql mysql:8.0
```

---

# 7. 시스템 정리

## 7.1 docker system prune

```bash
# 사용하지 않는 데이터 삭제
docker system prune

# 모든 이미지 포함
docker system prune -a

# 볼륨까지 삭제 (⚠️ 주의!)
docker system prune --volumes

# 확인 없이 즉시 삭제
docker system prune -af
```

---

# 8. Interview Readiness: 면접 대비

## ▶ Q1: Docker와 VM의 차이는?

**A**:

| 구분 | Docker | VM |
| --- | --- | --- |
| **시작 속도** | 초 단위 | 분 단위 |
| **리소스** | 경량 (호스트 OS 공유) | 무거움 (각자 OS) |
| **격리 수준** | 프로세스 수준 | OS 수준 |
| **이식성** | 높음 | 낮음 |

---

## ▶ Q2: docker run과 docker start의 차이는?

**A**:

- **docker run**: 이미지로부터 **새 컨테이너 생성 + 실행**
- **docker start**: 이미 생성된 **중지된 컨테이너 재시작**

```bash
# 처음 실행
docker run -d --name my-app nginx

# 중지
docker stop my-app

# 재시작 (설정 그대로 유지)
docker start my-app
```

---

## ▶ Q3: -v와 --mount의 차이는?

**A**:

```bash
# -v (간단, 오래된 방식)
docker run -v /host/path:/container/path nginx

# --mount (권장, 명시적)
docker run --mount type=bind,source=/host/path,target=/container/path nginx
```

**차이점**:

- `-v`: 간단하지만 모호함
- `--mount`: 명시적이고 오류 발견 쉽음 (권장)

---

## ▶ Q4: 컨테이너간 통신은 어떻게 하나요?

**A**: **같은 Docker 네트워크에 연결**하면 컨테이너 이름으로 통신 가능합니다.

```bash
# 1. 네트워크 생성
docker network create mynet

# 2. DB 컨테이너
docker run -d --name db --network mynet mysql

# 3. 앱 컨테이너
docker run -d --name app --network mynet \
  -e DB_HOST=db \
  myapp

# ✅ app에서 "db"로 접속 가능!
```

---

## 🔑 핵심 체크리스트

- [ ]  `docker run -d -p 8080:80 --name my-app nginx` 기본 패턴
- [ ]  `docker ps -a` 모든 컨테이너 확인
- [ ]  `docker logs -f` 실시간 로그 모니터링
- [ ]  `docker exec -it [container] bash` 컨테이너 접속
- [ ]  `docker network create` 컨테이너간 통신
- [ ]  `docker volume create` 데이터 영속성
- [ ]  `docker system prune -a` 정리

---

**작성일**: 2026-01-17  

**면접 빈출도**: ⭐⭐⭐ (상)