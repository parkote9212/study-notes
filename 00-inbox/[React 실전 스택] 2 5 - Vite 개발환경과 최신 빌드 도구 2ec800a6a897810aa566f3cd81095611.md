# [React 실전 스택] 2/5 - Vite 개발환경과 최신 빌드 도구

🏷️기술 카테고리: Build Tool, Frontend
💡핵심키워드: #설정관리, #성능최적화
💼 면접 빈출도: 상
⚖️ 의사결정(A vs B): No
날짜: 2026년 1월 18일 오후 4:12
📅 다음 복습일: 2026년 1월 25일

# 1. Abstract: 핵심 요약

> **Vite**(비트)는 2020년 Evan You가 만든 차세대 프론트엔드 빌드 도구로, **ES Modules**와 **esbuild**를 활용하여 기존 Webpack/CRA 대비 **10~100배 빠른 개발 서버**를 제공합니다. 2026년 현재 React 프로젝트의 사실상 표준이 되었습니다.
> 

**핵심 원칙**:

- 개발: ES Modules를 브라우저에서 직접 실행 (번들링 없음)
- 프로덕션: Rollup으로 최적화된 번들 생성
- HMR: 변경된 모듈만 즉시 교체

---

# 2. Vite 프로젝트 생성

## 2.1 React + TypeScript 프로젝트 생성

```bash
# Vite 프로젝트 생성
npm create vite@latest my-react-app -- --template react-ts

cd my-react-app
npm install
npm run dev
```

**생성된 폴더 구조**:

```
my-react-app/
├── node_modules/
├── public/
├── src/
│   ├── App.tsx
│   ├── main.tsx       # 진입점
│   └── vite-env.d.ts
├── index.html        # 루트 HTML
├── package.json
├── tsconfig.json
└── vite.config.ts    # Vite 설정
```

---

## 2.2 vite.config.ts 기본 설정

```tsx
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  
  // ✅ 경로 별칭 (@ = src)
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  
  // ✅ 개발 서버 설정
  server: {
    port: 3000,
    open: true,  // 브라우저 자동 오픈
    proxy: {
      // API 프록시
      '/api': {
        target: '[http://localhost:8080](http://localhost:8080)',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ''),
      },
    },
  },
  
  // ✅ 빌드 설정
  build: {
    outDir: 'dist',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
        },
      },
    },
  },
});
```

---

# 3. 환경변수 관리

## 3.1 .env 파일 구조

```bash
# .env.development (개발 환경)
VITE_API_URL=[http://localhost:8080](http://localhost:8080)
VITE_APP_TITLE=My App (Dev)

# .env.production (프로덕션)
VITE_API_URL=[https://api.example.com](https://api.example.com)
VITE_APP_TITLE=My App
```

**주의**: Vite는 `VITE_` 접두어가 있는 변수만 클라이언트에 노출

---

## 3.2 TypeScript로 타입 안전하게 사용

```tsx
// src/vite-env.d.ts
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_API_URL: string;
  readonly VITE_APP_TITLE: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
```

```tsx
// 사용 예시
const apiUrl = import.meta.env.VITE_API_URL;
const appTitle = import.meta.env.VITE_APP_TITLE;

console.log(`API: ${apiUrl}`);
```

---

# 4. 필수 플러그인

## 4.1 경로 별칭 (@) 설정

```bash
npm install -D @types/node
```

```tsx
// vite.config.ts
import path from 'path';

export default defineConfig({
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
      '@utils': path.resolve(__dirname, './src/utils'),
    },
  },
});
```

```tsx
// tsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@components/*": ["./src/components/*"],
      "@utils/*": ["./src/utils/*"]
    }
  }
}
```

**사용**:

```tsx
// ❌ Before
import Button from '../../../components/Button';

// ✅ After
import Button from '@components/Button';
```

---

## 4.2 SVG 컬포넌트로 사용

```bash
npm install -D vite-plugin-svgr
```

```tsx
// vite.config.ts
import svgr from 'vite-plugin-svgr';

export default defineConfig({
  plugins: [
    react(),
    svgr(),  // SVG를 React 컴포넌트로
  ],
});
```

```tsx
// 사용
import Logo from '@/assets/logo.svg?react';

const Header = () => <Logo width={100} height={50} />;
```

---

# 5. Vite vs Webpack/CRA

## 5.1 성능 비교

| 구분 | Vite | Webpack (CRA) |
| --- | --- | --- |
| **콜드 스타트** | 0.5초 | 10초 |
| **HMR** | 즉시 | 1-3초 |
| **빌드 속도** | 10초 | 60초 |
| **번들 크기** | 작음 | 큼 |

---

## 5.2 주요 차이점

**Vite**:

- ✅ ES Modules 기반 (Native ESM)
- ✅ esbuild로 초고속 분석
- ✅ 설정이 간단함
- ❌ IE11 미지원 (레거시 브라우저는 플러그인 필요)

**Webpack**:

- ✅ 성숙한 생태계
- ✅ 모든 브라우저 지원
- ❌ 설정이 복잡함
- ❌ 느린 개발 서버

---

# 6. 프로덕션 최적화

## 6.1 코드 스플리팅

```tsx
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        // ✅ 라이브러리별로 분리
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'router': ['react-router-dom'],
          'utils': ['axios', 'date-fns'],
        },
      },
    },
  },
});
```

---

## 6.2 Lazy Loading

```tsx
import { lazy, Suspense } from 'react';

// ✅ 동적 import
const Dashboard = lazy(() => import('@/pages/Dashboard'));
const Profile = lazy(() => import('@/pages/Profile'));

function App() {
  return (
    <Suspense fallback={<div>Loading...</div>}>
      <Routes>
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/profile" element={<Profile />} />
      </Routes>
    </Suspense>
  );
}
```

---

# 7. 실전 팔

## 7.1 에러 핸들링

```tsx
// vite.config.ts
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: '[http://localhost:8080](http://localhost:8080)',
        changeOrigin: true,
        // ✅ 에러 핸들링
        configure: (proxy, options) => {
          proxy.on('error', (err, req, res) => {
            console.log('Proxy error:', err);
          });
        },
      },
    },
  },
});
```

---

## 7.2 HTTPS 개발 서버

```bash
npm install -D @vitejs/plugin-basic-ssl
```

```tsx
import basicSsl from '@vitejs/plugin-basic-ssl';

export default defineConfig({
  plugins: [
    react(),
    basicSsl(),  // HTTPS 활성화
  ],
  server: {
    https: true,
  },
});
```

---

# 8. Interview Readiness

## ▶ Q1: Vite가 빠른 이유는?

**A**: 

1. **ES Modules 기반**: 번들링 없이 모듈을 브라우저에서 직접 실행
2. **esbuild**: Go로 작성된 초고속 번들러 (JavaScript 대비 100배 빠름)
3. **On-demand Compilation**: 필요한 파일만 컴파일
4. **효율적 HMR**: 변경된 모듈만 교체

---

## ▶ Q2: Vite의 프록시 설정을 사용하는 이유는?

**A**: CORS 문제를 해결하기 위함입니다. 프론트엔드([localhost:3000](http://localhost:3000))에서 백엔드([localhost:8080](http://localhost:8080))로 직접 요청하면 CORS 에러가 발생하지만, Vite 프록시를 통하면 **같은 도메인**으로 요청되어 CORS 문제가 없습니다.

---

## ▶ Q3: CRA에서 Vite로 마이그레이션하려면?

**A**:

1. `index.html`을 `public/`에서 루트로 이동
2. `%PUBLIC_URL%` → 제거
3. `process.env` → `import.meta.env`로 변경
4. `REACT_APP_` → `VITE_` 접두어로 변경
5. `vite.config.ts` 생성

---

## 🔑 핵심 체크리스트

- [ ]  Vite는 ES Modules + esbuild 기반
- [ ]  `npm create vite@latest`로 프로젝트 생성
- [ ]  환경변수는 `VITE_` 접두어 필수
- [ ]  `@` 별칭으로 경로 간소화
- [ ]  프록시로 CORS 문제 해결
- [ ]  manualChunks로 코드 스플리팅
- [ ]  Lazy Loading으로 초기 로듩 최적화

---

**작성일**: 2026-01-18  

**면접 빈출도**: ⭐⭐⭐⭐ (상)