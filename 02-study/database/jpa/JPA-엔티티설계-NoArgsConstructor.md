---
tags:
  - study
  - design-pattern
  - jpa
  - 디자인패턴
  - 클린코드
  - 프록시
created: 2026-01-23
difficulty: 상
---
# JPA-엔티티설계-NoArgsConstructor

🏷️기술 카테고리: Design Pattern, JPA
💡핵심키워드: #JPA, #디자인패턴, #클린코드, #프록시
💼 면접 빈출도: 상

# 1. Abstract: 핵심 요약

Lombok의 `@NoArgsConstructor(access = AccessLevel.PROTECTED)`는 JPA 엔티티 설계의 표준 관례로, 객체의 안정성과 프레임워크 요구사항 사이의 균형을 맞춥니다.

**핵심 원칙**:
- JPA는 리플렉션을 위해 기본 생성자 필요
- PROTECTED로 제한하여 외부의 무분별한 생성 차단
- Builder 또는 정적 팩토리 메서드로 안전한 생성 유도

# 2. 왜 기본 생성자가 필요한가?

## 2.1 JPA의 요구사항

JPA는 DB에서 데이터를 조회해 엔티티 객체를 생성할 때, **리플렉션(Reflection)** 기술을 사용하여 내부적으로 기본 생성자를 호출합니다.

만약 기본 생성자가 없으면 JPA는 객체를 생성하지 못하고 에러를 던집니다.

# 3. 왜 PROTECTED인가?

## 3.1 객체의 무분별한 생성 방지

**PUBLIC일 경우**:
- 개발자가 코드 어디서든 `new MemberTicket()`을 호출해 비어있는 객체를 만들 수 있음
- 데이터 정합성을 해치는 원인

**PROTECTED일 경우**:
- 외부 패키지에서는 `new` 키워드로 생성이 차단됨
- 엔티티 내부나 JPA 프록시 객체는 상속 구조를 통해 접근 가능

> "JPA 프록시 기술은 지원하면서, 개발팀은 new로 잘못 생성하지 마라"는 제약

## 3.2 의미 있는 생성 유도

무분별한 생성을 막고, Builder 또는 정적 팩토리 메서드를 통해서만 객체 생성하도록 강제합니다.

# 4. 코드 예시

```java
// ❌ 옛날 방식 (위험)
@Entity
public class MemberTicket {
    @Id @GeneratedValue private Long id;
    private int remainingCount;
    
    public MemberTicket() {}  // PUBLIC이면 무분별한 생성 가능
}

MemberTicket ticket = new MemberTicket();  // 비어있는 객체

// ✅ 모던 방식 (안전)
@Entity
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class MemberTicket {
    @Id @GeneratedValue private Long id;
    private int remainingCount;
    
    @Builder
    public MemberTicket(int remainingCount) {
        this.remainingCount = remainingCount;
    }
}

// MemberTicket ticket = new MemberTicket();  // ❌ 컴파일 에러!
MemberTicket ticket = MemberTicket.builder()
    .remainingCount(10)
    .build();  // ✅ 안전한 생성
```

# 5. 핵심 정리

**JPA는 왜 기본 생성자가 필요한가?**
- JPA가 리플렉션으로 객체를 생성할 때 필요
- Hibernate가 프록시 객체를 만들 때도 사용

**왜 protected로 제한하는가?**
- JPA 프록시 생성은 허용 (상속으로 접근 가능)
- 외부에서의 무분별한 new 호출은 차단
- Builder를 통한 생성 강제

**작성일**: 2026-01-23
**면접 빈출도**: ⭐⭐⭐⭐ (상)
